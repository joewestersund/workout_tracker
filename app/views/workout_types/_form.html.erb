<%= form_with(model: workout_type) do |form| %>
  <% if workout_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(workout_type.errors.count, "error") %> prohibited this workout_type from being saved:</h2>

      <ul>
        <% workout_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
