<%= form_with(model: model) do |form| %>
  <% if additional_data_type_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(additional_data_type_option.errors.count, "error") %> prohibited this additional_data_type_option from being saved:</h2>

      <ul>
        <% additional_data_type_option.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
